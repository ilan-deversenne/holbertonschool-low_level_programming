#include <stdio.h>

/**
 * main - Print number of elements
 * @argc: Number of arguments
 * @argv: Array with params
 *
 * Return: Always 0
 */
int main(int argc, __attribute__((unused))char *argv[])
{
	printf("%d\n", argc - 1);

	return (0);
}
